import { NgxLibmodalPipe } from './ngx-libmodal.pipe';

describe('NgxLibmodalPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxLibmodalPipe();
    expect(pipe).toBeTruthy();
  });
});
